# Claude Code 指南

## 项目定位

Bid Master Web：招投标智能分析工具箱

- 目标用户：招投标从业人员（招标方、投标方、代理机构）
- 核心价值：AI 驱动的招标文件智能提取、模拟编制、开标报价分析
- 架构：FastAPI 后端 + React 前端，多 AI 供应商支持

## 核心文档

一切决策必须参考：`.42cog/intent.md`（收敛方向，冲突时以它为准）

重大决策必须参考：

- `.42cog/intent.md` - 意向书：收敛方向、真难题、不做什么
- `.42cog/meta/` - 项目元数据、产品定位、商业模式
- `.42cog/real/` - 现实约束、安全规则
- `.42cog/cog/` - 认知模型、实体关系

> **分工线**：`.42cog/` 是**陈述句**（这个系统是什么样）；本文件是**祈使句**（你该怎么干）；
> `specs/` 是**标准**（产出该长什么样，含命名）。三者不许互相串门。

## 开发环境

| 项目     | 配置                         |
| -------- | ---------------------------- |
| 语言     | TypeScript（前端）+ Python（后端） |
| 包管理   | npm（前端）                   |
| Git 托管 | CNB（主仓库） |
| 运行时   | Node.js 18+ / Python 3.12+   |

## 项目结构

- app/ - Next.js 15 页面（App Router）
- src/frontend/ - 前端共享模块（组件/Hooks/Stores/工具库）
- src/backend/ - FastAPI 后端（Python）
- src/db/ - 数据库 Schema 和类型
- tests/ - 测试套件
- e2e/ - Playwright 端到端测试
- demo/ - 演示/原型代码
- docs/ - 文档（research/、bug-fix-summary/、plan/、error-log/、guide/、templates/）
- notes/ - 个人笔记
- chats/ - 对话记录
- specs/ - 项目规约（pm/、dev/、design/、style/）·产出标准
- resources/ - 外部参考（原 resource/，仅 README 入库）
- .42cog/ - 认知敏捷法文档（intent.md、meta/、real/、cog/、work/、others/）
- .42plugin/ - 本地技能库（42edu/）
- .42plugin.yml - 插件安装清单

## 规则

- 语言：代码注释、文档、提交信息、沟通全部使用中文
- 文件名：默认英文命名
- Git：不自动提交，除非用户明确要求
- 敏感信息：存 `.env.local`，绝不保存在 Git 仓库中
- 先冒烟、确认上下文够权限够
- 当 context 长度占用超过 70% 就自动使用 '/compact' 压缩对话

## 代码质量

- SOLID / DRY 原则
- 禁止 TODO 或临时方案，遇到时停下重新设计
- 死代码直接删除
- 开工前充分分析

## 自动化

- 使用 Makefile targets，不创建 shell 脚本
- 代码修改完成后，自动重启前后端服务（kill 端口 8000/3000 进程后重新启动）

## 部署与仓库

- 主生产环境：腾讯云服务器，由 systemd 管理
- 主代码仓库：CNB

## 四大原则（默认遵循）

1. 决策前置化：动手前先调研 + 冒烟测试，确认是否具有足够的信息和权限来完成任务。列出需要我拍板的关键决策点，对齐后才自主干活。把人介入前置到开工前。只要编写 Mastra 相关代码，必须遵循技能 .claude/skills/mastra

2. 状态持久化：唯一编码 + 状态外部化。给每个任务、文件设置唯一编码，方便追踪。任务进展不要只留在聊天记录中，而要写在外部文件中，文件即真相。通过这些文件，可以实现跨会话、上下文压缩后接续上一个任务（board / docs 编号 / state / changelog / memory）。

3. 能力弹性化：软技能 + 硬 CLI。软硬结合 + 技能编排；能固化成技能的经验固化到 skills/ 目录。

4. 工作并行化：通过 worktree 并行，控制在 4 个以内；派 worktree 之前先执行 `git fetch`，把远程最新代码拉取下来，确保基于最新版本后，才开始干。忽略文件无法进入 worktree，所以要么保证当前任务用不到这些忽略文件，要么在新建 worktree 时手动建一个软链接，指向主目录里已有的文件，避免重复下载或重复生成。

## 提交链标注

AI 或人机协作的提交，正文末尾加五行（照抄）：

```
Change-By:      human | ai | ai+human
Agent:          用的哪个工具
Model:          哪个模型
Suggested-By:   建议来自谁
Co-authored-by: 姓名 <邮箱>
```

作者亲手改的那些提交，就是日后喂给 AI 学你风格的唯一真相源。

## 铁律（补充）

- **唯一编码、不留副本**：同样的内容只留一份；引用只给路径 + 行号，不贴全文；不留 `-v2`/`-final` 类文件名，版本走提交链。
- **大文件不进库**：单文件超 10 MB 先想清楚，超 50 MB 一律不进——走 git-lfs 或放 NAS / 云盘，仓里只留路径 · 哈希 · 一句说明。
- **输出超三百 token 落盘**：长内容写文件后，对话里只留路径 + 一句摘要。
- **反意图污染**：整理人的口头指令时别扩写稀释——十个强信号扩成千字会稀释三倍，保护最强意图。
- **不自动提交 / 推送**，除非我明确要求。

## 文档

- 意向书 / 收敛方向：`./.42cog/intent.md`
- 项目规约：`./specs/`
- 普通文档：`./docs/`
- 部署文档：`./docs/deployment/`
- 工作记录：`./notes/yaojingbo`
